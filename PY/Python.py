#Opción a

prev = aux
while aux:
    if aux.dato!=valor:
        e1 = aux
        aux = aux.link
        prev = el.linke1.link = None

#Opción b
        
prev = aux
while aux:
    if aux.dato==valor:
        prev = aux
        aux = aux.link
        prev.link = None
    else:
        actual = actual.link
        actual = actual.link

#Opción c
        
e1 = aux
prev = aux
aux = aux.link
while aux:
    if
        aux.dato == valor:
        prev.link = aux.link
        aux.link = none
        aux = prev.link
    else:
        aux = prev
        prev = prev.link

#Opción d
        
e1 = aux
prev = aux
aux = aux.link
while aux:
    if aux.dato==valor:
        prev.link = aux.link
        aux.link = aux.link
        aux = prev.link
    else:
        aux = aux.link
        prev = prev.link
if e1.dato == ValueError
        e1.link = none


